- name: basic tools
  task_exec: !BashScript
    script:
    - "apt install -y tmux"
    - "apt install -y vim"
    - "apt install -y git"
    - "apt install -y htop"

- name: clang
  task_exec: !Bash
    command: "apt install -y clang"